/** インジェクション系レンズ: SQLi / コマンド / パストラバーサル / テンプレート */

import { composeSystemPrompt } from './common.js';
import type { Lens } from './types.js';

const SECTION = `## このレンズの観点: インジェクション
外部から来た値が、そのまま「解釈される文字列」として組み立てられていないかを見ます。

- **SQL / NoSQL インジェクション (CWE-89, CWE-943)**
  文字列連結・テンプレートリテラル・動的な ORDER BY / テーブル名・生クエリ実行。
  プレースホルダにバインドされている値は対象外。識別子（列名・テーブル名）は
  プレースホルダで守れないので、許可リストの有無を見る。
- **OSコマンドインジェクション (CWE-78)**
  シェルを介する実行（exec / system / シェル経由の spawn / バッククォート）に
  外部由来の値が混ざる場合。引数配列で渡していてシェルを経由しないなら、
  引数インジェクション（先頭のハイフン等）が問題になる場合だけ扱う。
- **パストラバーサル (CWE-22) / 意図しないファイルアクセス**
  外部由来の値がパス結合に入るケース。正規化後にベースディレクトリ配下かを
  検証しているか、拡張子や許可リストで絞っているかを見る。
- **テンプレートインジェクション (CWE-1336) / 式評価 (CWE-95)**
  テンプレート本文そのものを外部由来の値から組み立てている、
  eval / new Function / 動的 import に外部由来の値が渡っている、など。
  「テンプレートに値を差し込む」だけならXSS側の話なのでここでは扱わない。
- **LDAP / XPath / ヘッダ / ログ へのインジェクション** も同じ観点で見ます。

判断のポイントは「文字列が最終的にどのパーサに渡るか」と
「その手前でパーサの文法に対する無害化が行われているか」です。`;

export const injectionLens: Lens = {
  id: 'injection',
  title: 'インジェクション',
  systemPrompt: composeSystemPrompt(SECTION),
};
