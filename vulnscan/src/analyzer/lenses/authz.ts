/** 認証・認可レンズ: 認可欠落 / IDOR / 権限昇格 */

import { composeSystemPrompt } from './common.js';
import type { Lens } from './types.js';

const SECTION = `## このレンズの観点: 認証・認可
「誰が呼べるか」と「誰のデータを触れるか」が、コード上で担保されているかを見ます。

- **認証・認可チェックの欠落 (CWE-306, CWE-862)**
  外部から到達できる処理（HTTPルート、メッセージハンドラ、公開API）で、
  認証済みかどうか・権限を持つかどうかの確認が経路上のどこにも無いケース。
  ミドルウェア・デコレータ・フレームワークのガードで一括適用されている場合は
  それが有効なので、呼び出し元の文脈を確認してから判断してください。
- **不適切な認可 / IDOR (CWE-639, CWE-863)**
  リクエストで渡された識別子（ユーザーID、注文ID、ファイルID）を使って
  リソースを取得・更新しているのに、そのリソースが呼び出し元の所有物か・
  アクセス権があるかを検証していないケース。
  「認証は通っているが所有権を見ていない」パターンが中心です。
- **権限昇格 (CWE-269)**
  ロールや権限フラグをリクエスト由来の値から設定している、
  管理者判定がクライアント側の値に依存している、
  権限チェックの後に対象が差し替わる（TOCTOU）など。
- **信頼できない値による判定**
  ヘッダ・クッキー・クエリの値を検証なしに本人性の根拠にしている、
  署名や有効期限を検証せずにトークンを解釈している、など。

このレンズでは「チェックが無い」ことを主張するので、
経路上のどこを確認したうえで無いと言えるのかを reasoning に書いてください。
呼び出し元が提示されておらず、そこに認可がある可能性が残る場合は confidence を下げてください。`;

export const authzLens: Lens = {
  id: 'authz',
  title: '認証・認可',
  systemPrompt: composeSystemPrompt(SECTION),
};
